import { HotWordsDirective } from './hot-words.directive';

describe('HotWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new HotWordsDirective();
    expect(directive).toBeTruthy();
  });
});
