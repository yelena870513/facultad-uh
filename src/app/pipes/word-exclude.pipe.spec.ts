import { WordExcludePipe } from './word-exclude.pipe';

describe('WordExcludePipe', () => {
  it('create an instance', () => {
    const pipe = new WordExcludePipe();
    expect(pipe).toBeTruthy();
  });
});
