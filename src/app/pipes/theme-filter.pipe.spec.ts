import { ThemeFilterPipe } from './theme-filter.pipe';

describe('ThemeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
